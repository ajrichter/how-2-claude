{
  "name": "how-2-claude",
  "version": "0.1.0",
  "description": "Reference repo for Claude Code orchestrator/subagent patterns",
  "type": "module",
  "scripts": {
    "scan": "tsx examples/orchestrator.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ts": "vitest run tests/orchestrator.test.ts",
    "test:py": "pytest tests/test_orchestrator.py -v",
    "typecheck": "tsc --noEmit",
    "lint:py": "ruff check examples/ tests/ && ruff format --check examples/ tests/",
    "ci": "npm run typecheck && npm run test:ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.1",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
